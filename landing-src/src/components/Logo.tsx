export default function Logo() {
  return (
    <span
      aria-label="DMS"
      className="inline-flex items-center justify-center text-[13px] font-bold tracking-[0.12em] text-white"
    >
      DMS
    </span>
  );
}
